import { ApolloClient, gql, HttpLink, InMemoryCache } from 'apollo-boost';

export const _ApolloClient = ApolloClient;
export const _gql = gql;
export const _InMemoryCache = InMemoryCache;
export const _HttpLink = HttpLink;