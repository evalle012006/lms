import React from 'react'

function DropdownSelect({ options, selected, setSelected }) {
    return (
        <div>
            <span>{selected}</span>
        </div>
    )
}

export default DropdownSelect